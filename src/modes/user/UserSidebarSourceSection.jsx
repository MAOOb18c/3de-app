import UserSidebarSourceQuerySetup from "./UserSidebarSourceQuerySetup.jsx";
import UserSidebarSourceExecution from "./UserSidebarSourceExecution.jsx";

export default function UserSidebarSourceSection({ ctx, searchConditionProps }) {
  const {
    Bearer,
    DEFAULT_MAX_X_FETCH,
    JP_UI_LABELS,
    PUBLIC_PREVIEW_MAX_X_FETCH,
    PUBLIC_PREVIEW_MODE,
    QUERY_LABELS_JA,
    QUERY_LABEL_DESCRIPTIONS_JA,
    QueryReviewChips,
    STAGED_FETCH_INITIAL_COUNT,
    VOICE_DIRECTION_OPTIONS,
    Zone,
    adoptedHashtags,
    adoptedIncludeTerms,
    advice,
    afterAnalysisCandidateCount,
    afterDuplicateRate,
    afterNoiseRate,
    afterQuery,
    afterStatus,
    aiQueryAdvice,
    analysisCandidateCount,
    analysisPurpose,
    analysisPurposeLabel,
    autoFetchSafetyLimit,
    axis,
    axisConfig,
    axisLinkedKeywords,
    base,
    beforeAnalysisCandidateCount,
    beforeDuplicateRate,
    beforeNoiseRate,
    beforeQuery,
    beforeStatus,
    canAddImprovedQuery,
    canContinueStagedFetch,
    candidate,
    candidateCount,
    candidateRate,
    category,
    caution,
    cautionCandidates,
    cautionTerms,
    checked,
    clearAllXQueryCandidates,
    clusterPolicy,
    code,
    continueRemainingCount,
    continueStagedFetch,
    count,
    currentBatchCount,
    description,
    diagnosis,
    diagnosisStatus,
    diagnosisSummary,
    disabledCandidates,
    disabledTerms,
    duplicateRate,
    effectiveQuery,
    en,
    enabled,
    event,
    excludeLinks,
    excludeReplies,
    excludeRetweets,
    excludeTermCandidates,
    excludeTerms,
    excludeWords,
    externalOpinions,
    fallbackQuery,
    fallbackQueryLength,
    fetchX,
    fetchedCount,
    filter,
    finalQueryForXApi,
    finalQueryLength,
    finalTrustedQuery,
    focusManualQueryEdit,
    formatPercent,
    generatedBasicKeywords,
    handleFetchXButtonClick,
    has,
    hashtag,
    hashtagCandidates,
    help,
    improvedAddFetchCount,
    improvedQueryCandidates,
    improvedRefetchCount,
    improvementComparison,
    includeHashtags,
    includeTerms,
    includeWords,
    includes,
    index,
    inputSource,
    inputSourceLabel,
    is,
    isEffectiveQueryTooLong,
    isManualXQuery,
    item,
    ja,
    join,
    keyword,
    lang,
    language,
    lastAction,
    length,
    links,
    mainProblems,
    max,
    maxFetchCount,
    message,
    min,
    minLikes,
    minRetweets,
    min_likes,
    mode,
    nextValue,
    noiseBreakdown,
    noiseProcessingResult,
    noiseRate,
    noiseRelevanceThreshold,
    noiseRisk,
    normalizeVoiceDirections,
    normalizedCount,
    note,
    operationStatus,
    overallRetrievalQuality,
    personaMode,
    previous,
    problem,
    problemReasons,
    qualityLabel,
    query,
    queryAdjustment,
    queryAxisWarnings,
    queryBuildStatusLabel,
    queryBuildWarnings,
    queryDiagnosisLabel,
    queryDirty,
    queryLength,
    queryReview,
    queryReviewParts,
    queryTermDiagnosis,
    rawQuery,
    reason,
    recommendedQuery,
    refetchWithImprovedQuery,
    regenerateQueryFromCurrentInput,
    reply,
    resetAutoSummaryState,
    resetXQueryFilters,
    result,
    retrievalKpi,
    retrievalPolicy,
    retryDiagnosisWithWeakNoiseFilter,
    retweet,
    row,
    safeQuery,
    safeQueryPreview,
    safeRuntimeText,
    sanitizedExcludeRemovedParts,
    sanitizedExcludeTerms,
    sanitizedHashtagRemovedParts,
    sanitizedHashtags,
    selectAllXQueryCandidates,
    selectedAxisLinkedKeywords,
    selectedExcludeTermValues,
    selectedHashtagCandidates,
    selectedHashtagValues,
    selectedImprovedExecutionQuery,
    selectedImprovedQueryIndexes,
    selectedImprovedQueryIsSame,
    selectedVoiceDirections,
    selectedXQueryCandidateIds,
    selectedXQueryCandidates,
    selectionReason,
    selectionType,
    setActiveClusterRunId,
    setActiveDatasetId,
    setClusterSummaries,
    setExternalOpinions,
    setHasScoredWithCurrentAxis,
    setHistoryStatus,
    setIsManualXQuery,
    setQueryDirty,
    setSelectedXQueryCandidateIds,
    setSemanticClusterError,
    setSemanticClusterRows,
    setSemanticClusterStatus,
    setStagedFetchState,
    setXDataStatus,
    setXMaxResults,
    setXQuery,
    shouldShowStagedActions,
    stagedCurrentDataCount,
    stagedFetchEnabled,
    stagedFetchState,
    stagedNextFetchCount,
    stagedRemainingCount,
    status,
    stopReasonLabel,
    suggestion,
    target,
    term,
    themeCategory,
    toUpperCase,
    toggleAxisLinkedKeyword,
    toggleExcludeTermCandidate,
    toggleHashtagCandidate,
    toggleImprovedQueryCandidate,
    toggleVoiceDirection,
    toggleXQueryCandidate,
    totalApiFetchedCount,
    trim,
    ul,
    updateXQueryBase,
    updateXQueryFilter,
    userMessage,
    values,
    viewMode,
    voiceDirectionLabel,
    voicesToCollect,
    warning,
    weakNoiseRetryAvailable,
    x,
    xMaxResults,
    xQuery,
    xQueryBase,
    xQueryCandidates,
    xQueryFilters,
    xStatus,
    y,
    z,
    zone11Policy,
  } = ctx;

  return (
    <>
<section className="row card zone-card zone-3 x-query-zone">
        {viewMode === "developer" && <div className="zone-order-marker developer-panel-badge">表示順: 6 / Zone③</div>}
        <div className="field external-field">
          <h3>外部意見（1行1意見）</h3>
          <textarea
            value={externalOpinions}
            onChange={(event) => {
              setExternalOpinions(event.target.value);
              setActiveDatasetId("");
              setActiveClusterRunId("");
              setXDataStatus(event.target.value.trim() ? "unsaved" : "sample");
              setHasScoredWithCurrentAxis(false);
              setSemanticClusterRows([]);
              setSemanticClusterStatus("");
              setSemanticClusterError("");
              setClusterSummaries({});
              resetAutoSummaryState();
              setHistoryStatus("");
            }}
          />

          <div className="x-fetch-panel">
            <div className="zone-heading">{PUBLIC_PREVIEW_MODE ? "Zone③ 検索条件・意見取得" : "Zone③ 意見取得・検索クエリ"}</div>
            <p className="zone-lead">
              {PUBLIC_PREVIEW_MODE
                ? "テーマと評価軸に合った意見を取得します。評価軸に関連する語を選ぶと、検索条件に追加されます。"
                : "テーマと評価軸に合った意見を取得します。評価軸連動キーワードを選ぶと、実行クエリにOR条件で追加されます。"}
            </p>

            <UserSidebarSourceQuerySetup searchConditionProps={searchConditionProps} />
            <UserSidebarSourceExecution ctx={ctx} />
          </div>
        </div>
      </section>

      {/* データ詳細確認 */}
    </>
  );
}
